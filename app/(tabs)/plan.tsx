/**
 * Plan tab — Sprint 1 Block C
 *
 * Delegates to planner.tsx for now.
 * planner.tsx remains the canonical implementation file.
 * This shell will take on its own identity (Life Tracks, 30-Day System,
 * Friction Map sections) in a later sprint once those modules are built.
 */
import PlannerScreen from './planner';

export default PlannerScreen;

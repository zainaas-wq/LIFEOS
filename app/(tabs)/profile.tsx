/**
 * Profile tab — Sprint 1 Block C
 *
 * Delegates to settings.tsx for now.
 * settings.tsx remains the canonical implementation file.
 * This shell will expand to include the full Identity Engine
 * (life role, energy style, language selector, Life Tracks management)
 * in a later sprint.
 */
import SettingsScreen from './settings';

export default SettingsScreen;

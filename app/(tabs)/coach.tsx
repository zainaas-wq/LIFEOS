/**
 * Coach tab — Sprint 1 Block C
 *
 * Delegates to ai.tsx for now.
 * ai.tsx remains the canonical implementation file.
 * This shell will evolve into a full coaching console (strategy cards,
 * session history, voice-ready architecture) in a later sprint.
 */
import AIScreen from './ai';

export default AIScreen;
